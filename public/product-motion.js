function attachProductTilt(){if(window.matchMedia('(pointer: coarse)').matches||window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;document.querySelectorAll('.product-card:not([data-tilt])').forEach(card=>{card.dataset.tilt='true';card.addEventListener('pointermove',event=>{const rect=card.getBoundingClientRect();const x=(event.clientX-rect.left)/rect.width-.5;const y=(event.clientY-rect.top)/rect.height-.5;card.style.transform=`perspective(900px) rotateX(${y*-2.2}deg) rotateY(${x*2.8}deg) translateY(-5px)`});card.addEventListener('pointerleave',()=>{card.style.transform='';});});}new MutationObserver(attachProductTilt).observe(document.body,{childList:true,subtree:true});attachProductTilt();
